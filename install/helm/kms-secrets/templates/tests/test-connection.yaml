apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kms-secrets.fullname" . }}-test-connection"
  labels:
{{ include "kms-secrets.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "kms-secrets.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
