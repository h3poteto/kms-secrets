apiVersion: v1
kind: Service
metadata:
  name: {{ include "kms-secrets.fullname" . }}
  labels:
{{ include "kms-secrets.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "kms-secrets.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
